# 取扱説明書 — 公的機関 案件ボード

診断士（あなた）が応募できる**公的機関の専門家募集**を毎日自動で集めて、
合いそうな順に並べてくれるツールの使い方です。

---

## 1. これは何をしてくれる？

国・経産局・都道府県・市区町村・商工会議所・信用保証協会などに**バラバラに散らばっている**
「専門家募集・登録・委嘱」を、毎朝ロボットが見て回って1か所に集約します。

- あちこちのサイトを手で巡回しなくていい
- あなたのプロフィール（AI/DX専門・栃木拠点）に**合う順**に並ぶ
- **締切が近いもの**を教えてくれる（見落とすと1年待ちを防ぐ）

**あなたが普段やることは「1日1回ダッシュボードを見る」だけ**です。

---

## 2. 毎日の使い方

ダッシュボードを開くだけ 👉 **https://gucchi39.github.io/shindanshi_shigoto/**

毎朝6:30（JST）に自動更新されます。ブックマーク推奨。スマホでも見られます。

見る順番のおすすめ:
1. 上の**赤い「締切間近」バンド**を最初に確認（急ぎがあればここ）
2. **スコアが高い順**にカードをざっと見る
3. 気になったらタイトルをクリック → 本家サイトで応募要項を読む

---

## 3. 画面の見方

### 上部サマリー（4つの数字）
- **収集案件** … いま集まっている総数
- **募集中** … 明確に「募集中」と判定できた数
- **締切14日以内** … 急ぎ
- **高スコア(60+)** … あなた向き度が高いもの

### 締切間近バンド（赤い枠）
締切14日以内のものだけを、締切が近い順に表示。**本日締切**が最上部。

### 絞り込み・並び替え（チップ）
- **カテゴリチップ**（国／都県／市区町村／商工会議所／信用保証協会…）でしぼれる
- **スコア順／締切順**を切り替えられる

### 案件カード（1件ずつ）
| 見えるもの | 意味 |
|---|---|
| 左の大きい数字＋バー | **スコア**（0〜100、詳細は§4）。青=高、灰=中、薄灰=低 |
| タイトル（青くなる） | クリックで**本家サイトへ**。ここで応募要項を読む |
| 運営機関・エリア | どこが募集しているか |
| カテゴリのピル | 国／都県／市区町村／商工会議所／信用保証協会 など |
| 状態のピル | 緑=募集中／青=通年／灰=不明・締切 |
| 締切のピル | 朱=本日締切／琥珀=あとN日／灰=締切日 or 締切済 |
| 適合理由の文 | なぜこのスコアか（エリア一致・専門一致など） |
| ⚠ 年数要件 | 経験年数の条件が見つかった場合の**注意表示**（§4参照） |

---

## 4. スコアの意味

**「この案件があなたにどれだけ合っていそうか」を0〜100点で自動採点**したものです。
高いほど「あなた向きで、今応募できる可能性が高い」。

| 要素 | 満点 | 中身 |
|---|---:|---|
| エリア | 25 | 栃木/茨城/埼玉/東京に一致=25、全国=20、対象外=0 |
| 専門キーワード | 40 | 生成AI・DX・新規事業・生産性向上・製造業… の一致度（キーワード重み×一致数、上限40） |
| 募集ステータス | 20 | 募集中=20／通年=14／不明=8／次回見込み=4／締切=0 |
| LLM関連度 | 15 | ※APIキーを入れた時だけ。Claudeが「本当に専門家募集か」を判定して加点 |
| ネガティブ | −15 | 「職員採用」「工事」等のノイズ語が混じると減点 |

**目安**
- **60以上** … 優先チェック（エリア×専門がしっかり合致）
- 45〜59 … 関連あり・要確認
- 45未満 … 参考（状態不明や周辺情報など）

> いまはAPIキー未設定なので最大85点、多くが「状態=不明(8点)」です。
> `ANTHROPIC_API_KEY` を入れると関連度15点が加わり、精度が上がります（§6）。

### ⚠ 年数要件について（重要）
「登録後5年以上」などの**経験年数の条件はスコアに一切影響させません。除外もしません。**
`⚠年数要件` として**表示するだけ**です。理由は、要件を満たさなくても応募できる/例外がある
ことが多く、**勝手に消して見落とすのが一番もったいない**から。応募するかはあなたが判断してください。

---

## 5. 良さそうな案件を見つけたら

1. タイトルをクリックして**本家サイト**で応募要項を確認
2. 締切・応募方法・提出物をチェック
3. 応募する／しないを判断（年数要件が未充足でも、可否は問い合わせる価値あり）

※ このツールは「見つける」までを自動化します。応募そのものは各機関のサイトで行います。

---

## 6. あとから足せるオプション（任意）

### Slack通知（新着・締切を自動でSlackに飛ばす）
1. Slackで Incoming Webhook のURLを発行
2. リポジトリの **Settings → Secrets and variables → Actions → New repository secret**
3. 名前 `SLACK_WEBHOOK_URL`、値にURLを貼る

### 精度アップ（Claudeで中身を読む）
1. [console.anthropic.com](https://console.anthropic.com) でAPIキー発行（`sk-ant-...`、従量課金・少額）
2. 同じSecretsにNew secret、名前 `ANTHROPIC_API_KEY`、値にキー
3. 安く抑えたいなら `.github/workflows/crawl.yml` の `CLAUDE_MODEL: claude-haiku-4-5` の行を有効化

どちらも**入れなくても毎日の巡回・更新は動きます**（精度と通知のオプション）。

---

## 7. 自分好みにカスタマイズ

GitHub上でファイルを直接編集（鉛筆マーク）してコミットすればOK。次回巡回から反映されます。

### 監視する機関を追加/削除 → `config/sources.yaml`
```yaml
- id: 好きなID
  name: 機関名
  category: prefecture      # national/prefecture/city/cci/guarantee/incubation/other
  area: 栃木県
  url: https://…            # 監視するページ
  priority: high            # 重点監視の目印
```
`disabled: true` で一時停止、`watch_only: true` でページ更新のみ通知。

### 何を「合う」とみなすか → `config/profile.yaml`
- `keywords` … 専門キーワードと重み（数字を上げると効きが強くなる）
- `areas` / `area_keywords` … 活動エリア・近郊の地名
- `negative_keywords` … 減点したいノイズ語

### ノイズが混じる/本物が消える → `src/extract.ts`
リンク抽出のノイズ除外ルール。困ったら相談してください。

---

## 8. 困ったとき

| 症状 | 見るところ |
|---|---|
| 「取得失敗」が通知/レポートに出る | サイトの構造変更かURL切れ。`config/sources.yaml` のそのURLを確認・修正 |
| 案件が更新されない気がする | Actionsタブで `crawl` が緑になっているか確認。手動 Run workflow も可 |
| ダッシュボードが古い | Actionsタブで `deploy-pages` が動いたか確認。手動実行で即反映 |
| ノイズ（セミナー等）が混じる | `src/extract.ts` のノイズ除外を調整（相談OK） |
| 本物の募集が出てこない | `config/profile.yaml` のキーワード／`sources.yaml` の対象を見直す |

**「黙って0件」にはならない設計**です。取得に失敗したら失敗として通知されます。

---

## 9. どこに何がある（ファイル地図）

```
config/sources.yaml   … 監視する機関の一覧（57機関）
config/profile.yaml   … あなたのプロフィール＝マッチング基準
data/postings.json    … 収集結果（自動更新）
data/report.md        … Markdownレポート（GitHubでそのまま読める）
data/seen.json        … 既知案件の記録（差分検知用・触らない）
src/                  … 巡回・抽出・スコアリング・通知の本体
web/                  … ダッシュボード（Astro）
.github/workflows/    … crawl.yml（毎日巡回）/ deploy-pages.yml（ダッシュボード公開）
```

## 10. URL早見

- ダッシュボード: https://gucchi39.github.io/shindanshi_shigoto/
- レポート(Markdown): リポジトリの `data/report.md`
- リポジトリ: https://github.com/gucchi39/shindanshi_shigoto
